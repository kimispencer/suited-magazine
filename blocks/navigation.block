<nav>
	<ul>
		<span class="main-nav">
		{.repeated section items}
			<li>
				{.section collection}
					<a href="{fullUrl}">{navigationTitle}</a>
				{.end}
				{.section externalLink}
					<a href={url} {.section newWindow} target="_blank" {.end}>{title}</a>
				{.end}
			</li>
		{.end}
		</span>
		<span class="cart-nav">cart</span>
	</ul>
</nav>